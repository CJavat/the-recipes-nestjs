import { Scada, Inter } from 'next/font/google';

export const titleFont = Scada({ weight: ['400', '700'], subsets: ['latin'] });

export const inter = Inter({ weight: ['400', '700'], subsets: ['latin'] });