import { Scada, Inter } from 'next/font/google';

export const scada = Scada({ weight: ['400', '700'], subsets: ['latin'] });

export const titleFont = Inter({ weight: ['400', '700'], subsets: ['latin'] });